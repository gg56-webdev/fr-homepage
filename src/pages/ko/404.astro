---
import { changeLanguage, t } from "i18next";
import { NAV_LINKS } from "@/constants";
import Layout from "@/layouts/Layout.astro";

changeLanguage("ko");
---

<Layout>
  <section class="grid pt-36 place-items-center content-center gap-4 text-center">
    <h1 class="mb-4 text-4xl font-bold">{t('404')}</h1>
    <a href={NAV_LINKS.HOME.href} class="hover:underline">{t('common.go-home')} ↗</a>
  </section>
</Layout>
